/**
 * Windows OCR 预留（KE-034）
 *
 * - 契约：与 macOS 共用 `OcrEngine` / Fake contract
 * - 状态：reserved — 不下载模型、不实现 ONNX 推理
 * - 元数据：`artifacts/pp-ocr-v5-mobile-onnx.artifact.json`
 * - 运行时：`createWindowsOcrReservedStub()` → 恒 `OCR_UNAVAILABLE`
 */
